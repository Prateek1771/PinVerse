<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.1.0/fonts/remixicon.css"
    rel="stylesheet"/>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="w-full px-10 py-3 flex justify-between bg-zinc-800 text-white items-center">
        <a href="/feed" class="text-lg">PinVerse</a>
        <div class="flex gap-10">
            <a href="/profile">Profile</a>
            <a href="/feed">Feed</a>
            <a href="/logout" class="px-2 bg-red-700 text-white rounded-full">Logout</a>
        </div>
    </div>


    <div class="profile w-full min-h-screen bg-zinc-800 text-white pt-1">

        <form id="uploadform" hidden action="/fileupload" method="post" enctype="multipart/form-data">
            <input type="file" name="image">
        </form>

        <div class="profdets flex flex-col items-center mt-5 ">
            <div class="w-32 h-32 bg-zinc-100 rounded-full overflow-hidden">
            <img src="/images/uploads/<%= user.profileImage %>" id="uploadicon" class="w-full h-full object-cover" alt="">
            </div>
            <h1 class="text-3xl mt-2 font-semibold"><%= user.name %></h1>
            <h3 class="text-md">@<%= user.username %></h3>
            <a href="#" class="px-7 py-2 bg-zinc-700 rounded-full text-sm text-semibold mt-4">Edit</a>

            <a href="/add" class="px-10 py-2 rounded-lg bg-red-700 text-m font-semibold mt-5">Add New Post</a>
        </div>
        

        <div class="cards flex flex-wrap gap-10 px-10 mt-8">
            <div class="card">
                <div class="w-52 h-40 bg-zinc-200 rounded-lg overflow-hidden">
                    <img src="/images/uploads/<%= user?.posts[0]?.image %>" alt="" class="w-full h-full object-cover ">
                </div>
                <a href="/show/posts" class="inline-block text-xl font-semibold mt-3 ml-1">My Pins</h3>
                <h5 class="ml-1 text-sm font-medium opacity-60"><%= user.posts.length %> Pins</h5>
            </div>

        </div>
    </div>

    <script>
        document.querySelector("#uploadicon").addEventListener("click",function(){
            document.querySelector("#uploadform input").click();
        });

        document.querySelector("#uploadform").addEventListener("change",function(){
            document.querySelector("#uploadform").submit();
        })
    </script>
</body>
</html>